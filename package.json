{
  "name": "wsd-new",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.22.1",
    "react": "^16.3.1",
    "react-datepicker": "^1.4.1",
    "react-dom": "^16.3.1",
    "react-responsive": "^4.1.0",
    "react-scripts": "1.1.4",
    "react-slick": "^0.23.1",
    "smoothscroll-polyfill": "^0.4.3"
  },
  "engines": {
    "node": "8.11.3",
    "yarn": "1.9.4"
  },
  "scripts": {
    "start": "ls && cd wsd-server && ls && node wsd-api.js",
    "develop": "react-scripts start",
    "heroku-postbuild": "yarn install && yarn build && cd wsd-server && yarn install",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
